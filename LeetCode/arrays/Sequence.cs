namespace DataStructureAndAlgorithm.LeetCode.arrays
{
    public class Sequence
    {
        static public void LongestConsecutive(int[] numbers)
        {
            var max = numbers.Max();
            var length = Math.Ceiling(max/10m);
            var _numbers = new int[(int)length];

            for (int i = 0; i < numbers.Length; i++)
            {
                
            }
        }
    }
}
