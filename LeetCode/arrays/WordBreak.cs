namespace DataStructureAndAlgorithm.LeetCode.arrays
{
    public class WordBreak
    {
        static public void IsSequentialSegment(string word,IList<string> wordDict)
        {
            var searchIndex = 0;
            var _word = string.Empty;

            for (int i = 0; i < wordDict.Count; i++)
            {
                
            }
        }
    }
}
